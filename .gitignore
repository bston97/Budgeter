# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# OS cruft
Thumbs.db
.DS_Store

# Secrets and local config — never commit these
.env
.env.*
!.env.example

# Node — the app ships no build, but the tests run under node
node_modules/
npm-debug.log*

# Scratch / build output, should any appear
dist/
build/
*.log
